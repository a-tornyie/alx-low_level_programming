#include "main.h"
/**
 * add - adds two integers and returns the result
 * 1 if c is lowercase and 0 otherwisw
 * @a: input integer
 * @b: input integer
 *
 * Return: a+b
 */

int add(int a, int b)
{
	return (a + b);
}
